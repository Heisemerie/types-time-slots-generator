# @types/time-slots-generator

TypeScript definitions for [`time-slots-generator`](https://www.npmjs.com/package/time-slots-generator).

## Installation

```bash
npm install --save-dev @types/time-slots-generator
